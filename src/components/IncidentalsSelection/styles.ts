import { SxProps, Theme } from '@mui/material';
import { defaultColors } from '../../types/styles';

const styles: { [id: string]: SxProps<Theme> } = {
  list: { width: '100%', bgcolor: defaultColors.mainLight1 },
};

export default styles;
