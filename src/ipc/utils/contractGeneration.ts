import contract from '_/assets/contract/contractTemplate.md';

/**
 * Generates the contract as markdown string
 * @returns markdown string of the generated contract
 */
export function generateContractMarkdown(): string {
  return contract;
}
