describe('persistence', () => {
  afterEach(() => {
    jest.resetAllMocks();
  });

  describe('openDirectoryDialog', () => {
    test.todo('missing test');
  });

  describe('openFileDialog', () => {
    test.todo('missing test');
  });
});
