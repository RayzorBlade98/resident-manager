/**
 * Height of the app bar (in px)
 */
export const APPBAR_HEIGHT = 30;

/**
 * Width of the window content (in px)
 */
export const CONTENT_WIDTH = 1914;

/**
 * Height of the window content (in px)
 */
export const CONTENT_HEIGHT = 1051;

/**
 * Height of the window content without the app bar (in px)
 */
export const CONTENT_HEIGHT_WITHOUT_APPBAR = CONTENT_HEIGHT - APPBAR_HEIGHT;
